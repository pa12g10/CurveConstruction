#include "CashflowScheduler.h"

CashflowScheduler& CashflowScheduler::generateCashflows(const string& frequency)
{
	Date date_marker = Date(end_date.dd, end_date.mm, end_date.yyyy);

	/*while (date_marker >= start_date) {

		Date unadjusted_accural_start_date = Date();
		Date adjusted_accural_start_date = Date();
		Date unadjusted_accural_end_date = Date();
		Date adjusted_accural_end_date = Date();


	}*/

	return *this;
}
